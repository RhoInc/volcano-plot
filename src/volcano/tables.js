import init from './tables/init';
import layout from './tables/layout';
import drawSelected from './tables/drawSelected';
import drawDetails from './tables/drawDetails';

export var tables = {
    init: init,
    layout: layout,
    drawSelected: drawSelected,
    drawDetails: drawDetails
};
